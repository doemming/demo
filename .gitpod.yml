image:
  file: .gitpod.Dockerfile

tasks:
  - init: 'echo "TODO: Replace with init/build command"'
    command: 'docker-compose demo/docker-compose.yml up'
#   command: 'run --name deegree -d -p 8080:8080 deegree/deegree3-docker'
